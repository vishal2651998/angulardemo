import { ChatscrollDirective } from './chatscroll.directive';

describe('ChatscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatscrollDirective();
    expect(directive).toBeTruthy();
  });
});
